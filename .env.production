# 应用标题
VITE_APP_TITLE=简账

# 生产环境走同源，由 Nginx 反代 /api 到后端
VITE_BASE_API=/api

# 生产构建不设 mock
VITE_USE_MOCK=false

# 微信扫码登录：是否走真实后端链路（true=调 /api/auth/scan* 真接口）
# 需后端同时开启 app.wechat.enabled=true 并配置 AppID/Secret。
VITE_WECHAT_SCAN_ENABLED=false

# 默认不生成体积分析报告
VITE_REPORT=false
